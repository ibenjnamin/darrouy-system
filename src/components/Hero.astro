<section class="hero" id="inicio">
  <div class="container hero-grid">
    <div class="hero-copy">
      <div class="eyebrow">Software · Web · Integraciones · Automatización</div>

      <h1>
        Tecnología profesional para <span>hacer crecer y modernizar negocios.</span>
      </h1>

      <p>
        En DARROUY SYSTEM desarrollamos soluciones digitales enfocadas en empresas y organizaciones:
        sitios web profesionales, sistemas a medida, integraciones entre plataformas,
        automatización de procesos y soluciones conectadas a datos.
      </p>

      <div class="hero-actions">
        <a href="#contacto" class="btn btn-primary">Conversemos tu proyecto</a>
        <a href="#servicios" class="btn btn-secondary">Ver servicios</a>
      </div>
    </div>

    <div class="hero-visual">
      <div class="orb"></div>
      <div class="showcase">
        <div class="browser-bar"><span></span><span></span><span></span></div>
        <div class="screen">
          <div>
            <div class="screen-badge">DARROUY SYSTEM / DIGITAL SOLUTIONS</div>
            <div class="screen-lines">
              <div class="fake-title"></div>
              <div class="fake-line"></div>
              <div class="fake-line short"></div>
            </div>
          </div>
          <div class="fake-cards">
            <div class="fake-card"></div>
            <div class="fake-card"></div>
          </div>
        </div>
        <div class="floating-note">
          <strong>Desarrollo profesional</strong>
          <small>Web, software e integraciones.</small>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
.hero {
  position: relative;
  min-height: 82vh;
  display: flex;
  align-items: center;
  padding: 90px 0 80px;
}

.hero-grid {
  display: grid;
  grid-template-columns: 1.05fr 0.95fr;
  gap: 70px;
  align-items: center;
}

.eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 9px 14px;
  border-radius: 999px;
  border: 1px solid rgba(62, 157, 255, 0.24);
  background: rgba(31, 108, 255, 0.08);
  color: #80c8ff;
  font-size: 0.85rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  margin-bottom: 24px;
}

.eyebrow::before {
  content: "";
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--cyan);
  box-shadow: 0 0 20px var(--cyan);
}

h1 {
  font-size: clamp(3rem, 6.2vw, 6.1rem);
  line-height: 0.95;
  letter-spacing: -0.055em;
  max-width: 760px;
}

h1 span {
  background: linear-gradient(90deg, #fff 10%, #65b9ff 55%, #53f0ff);
  -webkit-background-clip: text;
  color: transparent;
}

.hero-copy p {
  margin-top: 28px;
  max-width: 690px;
  color: var(--muted);
  font-size: 1.12rem;
  line-height: 1.8;
}

.hero-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 14px;
  margin-top: 34px;
}

.hero-visual {
  position: relative;
  min-height: 500px;
  display: grid;
  place-items: center;
}

.orb {
  position: absolute;
  width: 420px;
  height: 420px;
  border-radius: 50%;
  background: radial-gradient(circle at center, rgba(32, 136, 255, 0.2), rgba(0, 88, 255, 0.03) 50%, transparent 72%);
  filter: blur(3px);
}

.showcase {
  position: relative;
  width: min(100%, 470px);
  padding: 28px;
  border: 1px solid rgba(71, 159, 255, 0.25);
  border-radius: 30px;
  background: linear-gradient(145deg, rgba(16, 49, 89, 0.76), rgba(4, 20, 40, 0.82));
  box-shadow: var(--shadow);
  backdrop-filter: blur(20px);
  transform: rotate(-2deg);
}

.browser-bar { display: flex; gap: 7px; margin-bottom: 22px; }
.browser-bar span {
  width: 9px; height: 9px; border-radius: 50%;
  background: rgba(255,255,255,0.22);
}

.screen {
  border-radius: 20px;
  border: 1px solid rgba(255,255,255,0.08);
  background:
    radial-gradient(circle at 75% 25%, rgba(31, 135, 255, 0.42), transparent 35%),
    linear-gradient(135deg, #07182d, #0e2850);
  min-height: 320px;
  padding: 32px;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.screen-badge {
  width: max-content;
  color: #89cfff;
  font-size: 0.8rem;
  border: 1px solid rgba(122, 193, 255, 0.3);
  border-radius: 999px;
  padding: 8px 12px;
}

.screen-lines { margin-top: 36px; }

.fake-title {
  width: 80%; height: 24px; border-radius: 99px;
  background: linear-gradient(90deg, #fff, #6fc9ff);
  opacity: 0.9;
}

.fake-line {
  width: 65%; height: 9px; border-radius: 99px;
  background: rgba(255,255,255,0.2);
  margin-top: 15px;
}
.fake-line.short { width: 50%; }

.fake-cards {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
  margin-top: 30px;
}

.fake-card {
  min-height: 90px;
  border: 1px solid rgba(255,255,255,0.09);
  border-radius: 16px;
  background: rgba(255,255,255,0.04);
}

.floating-note {
  position: absolute;
  right: -30px;
  bottom: 35px;
  padding: 18px 20px;
  border-radius: 17px;
  border: 1px solid rgba(68, 179, 255, 0.3);
  background: rgba(6, 29, 57, 0.9);
  box-shadow: 0 20px 50px rgba(0,0,0,0.25);
}

.floating-note strong { display: block; margin-bottom: 4px; }
.floating-note small { color: var(--muted); }

@media (max-width: 900px) {
  .hero-grid { grid-template-columns: 1fr; }
  .hero-visual { min-height: 420px; }
  .floating-note { right: 0; }
}

@media (max-width: 600px) {
  h1 { font-size: clamp(2.8rem, 15vw, 4.2rem); }
  .hero { min-height: auto; padding-bottom: 45px; }
  .showcase { transform: none; padding: 18px; }
  .floating-note { position: relative; margin-top: 15px; bottom: auto; }
}
</style>
